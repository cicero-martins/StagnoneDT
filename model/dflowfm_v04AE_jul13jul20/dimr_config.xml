<?xml version="1.0" encoding="utf-8"?>
<dimrConfig xmlns="http://schemas.deltares.nl/dimr"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://schemas.deltares.nl/dimr http://content.oss.deltares.nl/schemas/dimr-1.3.xsd">
  <documentation>
    <fileVersion>1.3</fileVersion>
    <createdBy>Stagnone DT — v03 coupled FM+SWAN (DelftIO streams)</createdBy>
  </documentation>
  <control>
    <parallel>
      <startGroup>
        <!-- 2026-05-11: progressao 600 -> 1800 -> 3600 s entre chamadas SWAN.
             O bottleneck e a CPU-cost por chamada SWAN nested estacionaria (68x53 inner +
             outer, ~100s/call no Xeon 6430 com 16 threads OpenMP). Threads ja saturaram
             em 16 (32 piora), entao ganho adicional vem de chamar SWAN menos vezes.
             Trade-off: feedback wave-current a cada 1h sim em vez de 30 min. Para
             lagoa tidal 12.4h e sea breeze diurnal isso e aceitavel. Se validacao
             de drifter (wave-driven Stokes/windage) sofrer, reverter para 1800. -->
        <time>0 3600 691200</time>
        <start name="wave"/>
      </startGroup>
      <start name="DFlowFM"/>
    </parallel>
  </control>
  <component name="DFlowFM">
    <library>dflowfm</library>
    <process>0 1 2 3 4 5 6 7</process>
    <workingDir>.</workingDir>
    <inputFile>Stagnone_dxy01_15m.mdu</inputFile>
    <mpiCommunicator>DFM_COMM_DFMWORLD</mpiCommunicator>
  </component>
  <component name="wave">
    <library>wave</library>
    <workingDir>wave</workingDir>
    <inputFile>stagnone.mdw</inputFile>
  </component>
</dimrConfig>
