# Copy to `.env` and fill with credentials from:
#   https://datalab.dive.edito.eu/account/storage
# These are temporary — renew when the session expires.
# DO NOT COMMIT .env to git.

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_SESSION_TOKEN=
AWS_S3_ENDPOINT=https://minio.dive.edito.eu
AWS_DEFAULT_REGION=us-east-1
